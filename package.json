{
  "name": "git-memento",
  "private": true,
  "scripts": {
    "build:action": "tsc -p tsconfig.json",
    "typecheck:action": "tsc -p tsconfig.json --noEmit",
    "test:js": "npm run build:action && node --test tests-js/note-comment-renderer.test.js"
  },
  "dependencies": {
    "markdown-it": "^14.1.1"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
